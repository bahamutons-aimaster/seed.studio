[build]
  publish = "public"
  functions = "netlify/functions"
  command = "echo 'No build step needed'"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[dev]
  publish = "public"
  functions = "netlify/functions"
